
## Release Build API Key Reminder
When the user asks to build a release bundle (e.g., APK, AppBundle, or IPA), you MUST remind them to include the `--dart-define=API_KEY=<production_key>` flag in the build command. Ask them if they have their production API key ready before executing the build.
